table:
  name: users
  schema: public
configuration:
  custom_column_names:
    created_at: createdAt
    updated_at: updatedAt
  custom_root_fields: {}
select_permissions:
- permission:
    columns:
    - created_at
    - email
    - id
    - name
    - updated_at
    filter:
      id:
        _eq: X-Hasura-User-Id
  role: user
update_permissions:
- permission:
    check: null
    columns:
    - email
    - name
    filter:
      id:
        _eq: X-Hasura-User-Id
  role: user
